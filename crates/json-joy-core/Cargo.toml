[package]
name = "json-joy-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core Rust primitives for json-joy-rs"

[dependencies]
rand = "0.8"

[dev-dependencies]
serde_json = "1.0"
