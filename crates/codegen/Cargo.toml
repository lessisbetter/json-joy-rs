[package]
name = "json-joy-codegen"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Code generation utilities for json-joy (stub - runtime JS codegen not applicable in Rust)"

[dependencies]

[features]
default = []
